{
  "schemaVersion": 1,
  "id": "${modId}",
  "version": "${version}",
  "name": "Necron UI",
  "mixins": [
    "${modId}.mixins.json"
  ],
  "depends": {
    "java": "*",
    "minecraft": "*",
    "fabricloader": "*"
  }
}
